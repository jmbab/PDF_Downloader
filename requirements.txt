# Core libraries for data handling, HTTP requests, and Excel file manipulation
pandas==2.2.3
requests==2.32.0  # Upgraded to resolve pip-audit vulnerability GHSA-9wx4-h78v-vm56, but version is yanked due to CVE-2024-35195. Keep an eye for updates.
openpyxl==3.1.0
PyPDF2==3.0.0
numpy==2.1.3  # Updated to a compatible version for Python 3.12
pytest
